# Example for future ORM integration (SQLAlchemy)
# Currently placeholder
class ServerMetric:
    def __init__(self, name, cpu, memory):
        self.name = name
        self.cpu = cpu
        self.memory = memory
